{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile and role information",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["admin", "user"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "role"]
    },
    "Signal": {
      "title": "Signal",
      "description": "Trading signal generated by Sentinel",
      "type": "object",
      "properties": {
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "type": { "type": "string" },
        "symbol": { "type": "string" }
      },
      "required": ["content", "timestamp"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "Chat message between user and AI",
      "type": "object",
      "properties": {
        "role": { "type": "string", "enum": ["user", "ai"] },
        "content": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["role", "content", "timestamp"]
    },
    "TelegramChat": {
      "title": "TelegramChat",
      "description": "Chat history for a specific Telegram user",
      "type": "object",
      "properties": {
        "history": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "role": { "type": "string", "enum": ["user", "model"] },
              "content": { "type": "string" }
            },
            "required": ["role", "content"]
          }
        },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["history"]
    },
    "JournalEntry": {
      "title": "JournalEntry",
      "description": "Trading journal entry",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "symbol": { "type": "string" },
        "side": { "type": "string" },
        "entryPrice": { "type": "number" },
        "exitPrice": { "type": "number" },
        "stopLoss": { "type": "number" },
        "target1": { "type": "number" },
        "target2": { "type": "number" },
        "pnl": { "type": "number" },
        "reason": { "type": "string" },
        "sentiment": { "type": "string" },
        "status": { "type": "string" },
        "source": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["symbol", "side", "pnl", "timestamp"]
    },
    "JournalSummary": {
      "title": "JournalSummary",
      "description": "Auto-generated 12-hour trading journal summary",
      "type": "object",
      "properties": {
        "timestamp": { "type": "string", "format": "date-time" },
        "period": { "type": "string" },
        "totalTrades": { "type": "number" },
        "winRate": { "type": "number" },
        "netPnl": { "type": "number" },
        "tradedSymbols": {
          "type": "array",
          "items": { "type": "string" }
        },
        "evaluation": { "type": "string" }
      },
      "required": ["timestamp", "period", "totalTrades", "winRate", "netPnl", "evaluation"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles and roles"
    },
    "/signals/{signalId}": {
      "schema": { "$ref": "#/entities/Signal" },
      "description": "Stores all trading signals"
    },
    "/chats/{chatId}": {
      "schema": { "$ref": "#/entities/ChatMessage" },
      "description": "Stores chat history"
    },
    "/telegram_chats/{chatId}": {
      "schema": { "$ref": "#/entities/TelegramChat" },
      "description": "Stores Telegram chat history for Sentinel"
    },
    "/trading_journal/{journalId}": {
      "schema": { "$ref": "#/entities/JournalEntry" },
      "description": "Stores trading journal entries"
    },
    "/journal_summaries/{summaryId}": {
      "schema": { "$ref": "#/entities/JournalSummary" },
      "description": "Stores 12-hour auto-generated journal summaries"
    }
  }
}
